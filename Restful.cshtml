@using System.IO;
@using System.Text;

@{
    string serverID = String.Empty;
    string imagegID = String.Empty;
    string secret = String.Empty;
    string farmID = String.Empty;
    string uri = "http://api.flickr.com/services/rest/?method=flickr.favorites.getPublicList&api_key={your-api-key}&user_id={your-user_id}";   

    HttpWebRequest request = (HttpWebRequest)WebRequest.Create(uri);     
    WebResponse response = request.GetResponse();
    Stream strm = response.GetResponseStream();
    XmlTextReader reader = new XmlTextReader(strm); 

}
